<Page
    x:Class="MovieApp.Ui.Views.SectionEventsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:MovieApp.Ui.Controls"
    mc:Ignorable="d">

    <ScrollViewer Padding="24" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16">
            <Button
                HorizontalAlignment="Left"
                Click="BackButton_Click"
                Content="← Back" />

            <TextBlock
                Style="{ThemeResource HeaderTextBlockStyle}"
                Text="{Binding PageTitle}" />

            <TextBlock
                Opacity="0.7"
                Text="All events in this section" />

            <ItemsControl ItemsSource="{Binding VisibleEvents}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Margin="0,0,0,12"
                            Padding="0"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="EventCardButton_Click">
                            <ui:EventCard Model="{Binding}" />
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>