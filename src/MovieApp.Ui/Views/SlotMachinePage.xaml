<Page
    x:Class="MovieApp.Ui.Views.SlotMachinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="20">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="16">
                <Border Style="{StaticResource SurfaceCardStyle}">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource StrongSurfaceCardStyle}" MinHeight="180" />
                        <Border Grid.Column="1" Style="{StaticResource StrongSurfaceCardStyle}" MinHeight="180" />
                        <Border Grid.Column="2" Style="{StaticResource StrongSurfaceCardStyle}" MinHeight="180" />
                    </Grid>
                </Border>

                <Border Style="{StaticResource SurfaceCardStyle}">
                    <ListView Height="220" />
                </Border>
            </StackPanel>

            <Border
                Grid.Column="1"
                Style="{StaticResource SurfaceCardStyle}">
                <StackPanel Spacing="10">
                    <TextBox Header="Available spins" />
                    <TextBox Header="Bonus spins today" />
                    <TextBox Header="Open streak" />
                    <Button Content="Spin" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
